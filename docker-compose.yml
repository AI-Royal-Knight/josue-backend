services:
  web:
    build: .
    container_name: josue_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    # Uses your existing .env file for environment variables
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000
    restart: unless-stopped
